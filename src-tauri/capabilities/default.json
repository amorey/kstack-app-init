{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and any additional windows opened via File → New Window (labels `window-N`).",
  "windows": ["main", "window-*"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}

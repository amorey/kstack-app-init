# Escape hatch for the `Rust · Audit` CI job: an advisory listed here stops
# failing the build. Every entry names its advisory and why it is accepted,
# so the list stays reviewable. Prefer an upgrade — an ignore is a standing risk.
[advisories]
ignore = []

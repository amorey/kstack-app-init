schema:
  - graph/*.graphqls

exec:
  filename: graph/generated.go
  package: graph

model:
  filename: graph/model/models_gen.go
  package: model

resolver:
  layout: follow-schema
  dir: graph
  package: graph
  filename_template: "{name}.resolvers.go"

autobind:
  - github.com/kubetail-org/kstack-app/sidecar/graph/model

models:
  # --- KubeConfig ---
  KubeConfig:
    model: github.com/kubetail-org/kstack-app/sidecar/graph/model.KubeConfig
    fields:
      authInfos:
        resolver: true
      clusters:
        resolver: true
      contexts:
        resolver: true

  KubeConfigAuthInfo:
    model: github.com/kubetail-org/kstack-app/sidecar/graph/model.KubeConfigAuthInfo

  KubeConfigCluster:
    model: github.com/kubetail-org/kstack-app/sidecar/graph/model.KubeConfigCluster

  KubeConfigContext:
    model: github.com/kubetail-org/kstack-app/sidecar/graph/model.KubeConfigContext

  KubeConfigExtensions:
    model: github.com/kubetail-org/kstack-app/sidecar/graph/model.KubeConfigExtensions

  # --- Cluster ---
  # The GraphQL Cluster types bind straight to the controllers domain types.
  # ClusterStatus is a thin model.ClusterStatus wrapper (embedded
  # controllers.ClusterStatus + the ClusterID) because its cache field resolver
  # needs the cluster's ID and a child resolver only sees its own obj.
  Cluster:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/cluster.Cluster
    fields:
      status:
        resolver: true

  ClusterSpec:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/cluster.ClusterSpec

  ClusterSource:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/cluster.ClusterSource

  ClusterSourceKubeconfig:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/cluster.ClusterSourceKubeconfig

  ClusterSourceStatus:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/cluster.ClusterSourceStatus

  ClusterStatus:
    model: github.com/kubetail-org/kstack-app/sidecar/graph/model.ClusterStatus
    fields:
      cache:
        resolver: true

  ClusterServer:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/cluster.ClusterServer

  ClusterKubeconfig:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/cluster.KubeconfigStatus

  ClusterPrincipal:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/cluster.ClusterPrincipal
    fields:
      permissions:
        resolver: true

  # The conditions ride the wire as-is (the controllers domain shapes; the
  # webview derives display state from type/status/reason).
  ClusterCondition:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/cluster.ClusterCondition

  ConditionStatus:
    model: github.com/amorey/beehive.ConditionStatus

  ClusterSyncStatus:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/cluster.ClusterCacheStatus

  ClusterCache:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/cluster.CacheStats

  CachedResource:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/cluster.CachedResourceStats

  # --- Auth ---
  # Bind the GraphQL auth types straight to the auth domain types so the
  # resolvers need no field-by-field mapper. Only schema fields are exposed —
  # State.Tokens never reaches the wire — and Identity's `sub` claim maps onto
  # the domain's UserID field.
  AuthState:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/auth.State

  Identity:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/auth.Identity
    fields:
      sub:
        fieldName: UserID

  # --- gqlgen ---
  ID:
    model:
      - github.com/99designs/gqlgen/graphql.ID
      - k8s.io/apimachinery/pkg/types.UID
      - github.com/kubetail-org/kstack-app/sidecar/internal/cluster.ClusterID

  Int:
    model:
      - github.com/99designs/gqlgen/graphql.Int
      - github.com/99designs/gqlgen/graphql.Int64
      - github.com/99designs/gqlgen/graphql.Int32

  String:
    model:
      - github.com/99designs/gqlgen/graphql.String
      - github.com/kubetail-org/kstack-app/sidecar/internal/cluster.ClusterConditionType

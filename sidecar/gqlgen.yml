schema:
  - graph/*.graphqls

exec:
  filename: graph/generated.go
  package: graph

model:
  filename: graph/model/models_gen.go
  package: model

resolver:
  layout: follow-schema
  dir: graph
  package: graph
  filename_template: "{name}.resolvers.go"

autobind:
  - github.com/kubetail-org/kstack-app/sidecar/graph/model

models:
  # --- Cluster ---
  # The GraphQL Cluster types bind 1:1 (same name) into internal/clustersvc/types —
  # gqlgen autobinds with no wrapper. The schema mirrors beehive's
  # owner chain: Cluster.status is the Cluster kind's stored status, and the
  # ClusterCache child carries the ClusterCache kind's sync status (`status`) plus
  # live on-disk `stats` (a resolver, so a query pays the stat cost only when it
  # selects them). The ClusterCache child carries the cluster ID for that resolver.
  Cluster:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.Cluster
    fields:
      # Both are separate beehive reads, kept off the record and run only when
      # selected: event chatter must not ride the cluster watch, and the owned
      # caches stream standalone so cache churn never re-emits a cluster.
      caches:
        resolver: true
      events:
        resolver: true

  ClusterSpec:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterSpec

  ClusterSpecSource:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterSpecSource

  ClusterSpecSourceKubeconfig:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterSpecSourceKubeconfig

  ClusterStatusSource:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterStatusSource

  ClusterStatus:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterStatus

  ClusterServer:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterServer

  ClusterStatusSourceKubeconfig:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterStatusSourceKubeconfig

  ClusterPrincipal:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterPrincipal
    fields:
      permissions:
        resolver: true

  # The conditions ride the wire as-is (the stored shapes; the webview derives
  # display state from type/status/reason). Unprefixed shared vocabulary: the same
  # Condition rides both the Cluster kind's status and the ClusterCache kind's, so
  # it belongs to no single storage provenance.
  #
  # Condition/Event/Schedule are shared, kind-agnostic vocabulary on the wire but
  # live in `internal/clustersvc` only because it's their sole consumer today. The
  # wire-universal (unprefixed) name and the internal package home are orthogonal:
  # the unprefixed name says "not specific to one wire namespace", not "must live
  # package-neutral". When a non-cluster kind/subsystem first needs conditions,
  # hoist all three together into a shared leaf package (e.g. internal/apimeta),
  # leaving cluster its ConditionType constants (Connected/Healthy/Synced) — see
  # sidecar/CLAUDE.md. Until that second consumer exists, a shared package would be
  # a one-importer abstraction, so they stay here.
  Condition:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.Condition

  # The generic event surface. Event binds 1:1 to the Go type; its run id
  # rides the shared ObjectID scalar (no dedicated Event id scalar), and EventType
  # binds straight to beehive.EventType (values Normal/Warning match), the same
  # external-enum pattern as ConditionStatus.
  Event:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.Event

  # The event watches' envelope. Its own two-value enum rather than DeltaFrameType: the
  # log is append-only, so Added/Modified/Deleted would advertise transitions that
  # can never arrive.
  EventWatchFrame:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.EventWatchFrame

  EventFrameType:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.EventFrameType

  EventType:
    model: github.com/amorey/beehive.EventType

  # The generic reconcile-schedule gauge (next-requeue countdown), decoupled from
  # the cluster/list watch. Binds 1:1 to the Go type.
  Schedule:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.Schedule

  ConditionStatus:
    model: github.com/amorey/beehive.ConditionStatus

  # The delta watches. DeltaFrameType binds to types.DeltaFrameType (values
  # Added/Modified/Deleted match the enum), the same external-enum pattern as
  # EventType/ConditionStatus — a defined type mirroring beehive's ChangeType, not that
  # type itself (it aliases into an internal package gqlgen can't import). The frame
  # wrappers bind 1:1 to the types the watch pumps emit.
  DeltaFrameType:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.DeltaFrameType

  ClusterWatchFrame:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterWatchFrame

  ClusterCacheWatchFrame:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterCacheWatchFrame

  ClusterCache:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterCache
    fields:
      stats:
        resolver: true
      # kinds and events are separate reads (the cache's SQLite catalog, the beehive
      # event log), so both stay off the record and run only when selected.
      kinds:
        resolver: true
      syncs:
        resolver: true
      events:
        resolver: true

  # The sync children's served spec IS the stored beehive type (no projection layer).
  # Their `stats` are NOT: those are resolver fields sampled from the controller, because
  # status is a propagation channel and nothing in the object graph reads them.

  # Likewise the GVR-discovery child: stored spec/status served as-is.
  ClusterCachedCatalog:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterCachedCatalog
    fields:
      # Sampled from the controller on read, like ClusterCache.stats — it is not a field
      # of the stored object (the kind's status is empty by design).
      stats:
        resolver: true
  ClusterCachedResource:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterCachedResource
    fields:
      # Same shape as the discovery's: a controller-held gauge, not stored state.
      stats:
        resolver: true
      events:
        resolver: true
  ClusterCachedResourceSpec:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterCachedResourceSpec
  ClusterCachedResourceStats:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterCachedResourceStats
  ClusterCachedResourceWatchFrame:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterCachedResourceWatchFrame
  ClusterCacheSyncHealth:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterCacheSyncHealth
  SyncedKindRef:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.SyncedKindRef

  ClusterCachedCatalogSpec:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterCachedCatalogSpec

  ClusterCachedCatalogStats:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterCachedCatalogStats

  ClusterCachedCatalogWatchFrame:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterCachedCatalogWatchFrame

  ClusterCacheStats:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterCacheStats

  ClusterCacheResourceStats:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterCacheResourceStats

  ClusterCachedDataKind:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterCachedDataKind

  ClusterCachedDataKindWatchFrame:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterCachedDataKindWatchFrame

  ClusterCachedDataEvent:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterCachedDataEvent
    # The record carries value time.Time (must stay comparable for the delta-watch diff),
    # so resolvers map the zero time to null instead of serializing 0001-01-01.
    fields:
      firstSeen:
        resolver: true
      lastSeen:
        resolver: true

  ClusterCachedDataEventWatchFrame:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterCachedDataEventWatchFrame

  ClusterCachedDataObject:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterCachedDataObject
    # The record carries value time.Time (parallel to ClusterCachedDataEvent), so the resolver
    # maps the zero creationTimestamp to null instead of serializing 0001-01-01. rawJSON
    # autobinds to the RawJSON field (no resolver): the watch already reads the body for
    # every row — the delta-diff needs it in the comparable struct — so gating its marshal
    # behind a resolver would defer nothing, and field-selection already skips it when a
    # query omits rawJSON.
    fields:
      creationTimestamp:
        resolver: true

  ClusterCachedDataObjectWatchFrame:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ClusterCachedDataObjectWatchFrame

  # --- Auth ---
  # Bind the GraphQL auth types straight to the auth domain types so the
  # resolvers need no field-by-field mapper. Only schema fields are exposed —
  # State.Tokens never reaches the wire — and Identity's `sub` claim maps onto
  # the domain's UserID field.
  AuthState:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/auth.State

  Identity:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/auth.Identity
    fields:
      sub:
        fieldName: UserID

  # The object-identity scalar binds to the typed int64 ObjectID (its
  # MarshalGQL/UnmarshalGQL handle the decimal-string wire form), so resolver
  # arguments arrive already typed — no per-resolver parsing. ClusterID (and
  # future per-kind ids) are aliases of this one type, sharing its machinery.
  ObjectID:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ObjectID

  # The JSON scalar binds to clustersvc.RawJSON — a comparable string carrying already-
  # serialized JSON, marshaled verbatim (see rawjson.go). It backs a cached object's
  # full native body on ClusterCachedDataObject.
  JSON:
    model: github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.RawJSON

  # --- gqlgen ---
  ID:
    model:
      - github.com/99designs/gqlgen/graphql.ID
      - k8s.io/apimachinery/pkg/types.UID

  Int:
    model:
      - github.com/99designs/gqlgen/graphql.Int
      - github.com/99designs/gqlgen/graphql.Int64
      - github.com/99designs/gqlgen/graphql.Int32

  String:
    model:
      - github.com/99designs/gqlgen/graphql.String
      - github.com/kubetail-org/kstack-app/sidecar/internal/clustersvc/types.ConditionType
